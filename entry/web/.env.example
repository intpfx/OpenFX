# OpenFX Console production requirements. Replace both values in the deploy
# platform's encrypted environment settings; never commit real credentials.
OPENFX_ADMIN_KEY=
OPENFX_NODE_CREDENTIAL_KEY=

# Optional existing OpenFX Web settings.
# DOWNIP_REDIRECT_SCHEME=https
# DOWNIP_REDIRECT_PORT=443
# OPENFX_PROXY_UPSTREAM=https://fixed-upstream.example
