//#region src/index.ts
/** Host loader entry for the browser-only conversation view plugin. */
/** Stable Cordis plugin name. */
const name = "conversation-browser";
/** Provides no host-side behavior. */
function apply() {}
//#endregion
export { apply, name };
