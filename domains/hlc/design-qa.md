# HLC 设计验收：统一导航组件与内嵌账户入口

## 验收对象

- 最初需要淘汰的全屏账户页：
  `/var/folders/6v/flkn8c1j3wgfd0tmjbpnl56h0000gn/T/codex-clipboard-a1d477ab-1b88-4324-8b27-f170f0b5b106.png`
  （2450 × 1276）
- 目标承载位置“场所一览”：
  `/var/folders/6v/flkn8c1j3wgfd0tmjbpnl56h0000gn/T/codex-clipboard-b28718a6-fc3e-46d0-956e-dbdbf632ea35.png`
  （742 × 906）
- 统一组件前的浏览器状态： `docs/qa/phase-15-nav-morph-before.jpg`（1280 × 720）
- 统一组件后的桌面实现： `docs/qa/phase-15-nav-morph-after.jpg`（1280 × 720）
- 统一组件后的移动端实现： `docs/qa/phase-15-nav-morph-mobile.jpg`（390 × 844）
- 登录说明删除前／后： `docs/qa/phase-15-login-helper-before.jpg`、
  `docs/qa/phase-15-login-helper-after.jpg`（均为 1280 × 720）

桌面对照使用相同的 1280 × 720 CSS 视口、1 倍密度、访客身份和登录状态。移动端使用
390 × 844 CSS 视口、1
倍密度。两张早期参考图裁切和密度不同，因此只用于确认组件职责与
承载位置，不进行整幅逐像素重叠。

## 验收状态

- 访客身份、未登录。
- 已进入社区地图。
- 验证导航组件的收起、场所列表和登录／注册三种状态。
- 默认显示登录页，并验证居民注册、返回和关闭复位。

## Findings

- P0：无。
- P1：无。
- P2：无。
- P3：无阻断项。

## 全局视图比较

- 调整前：账户面板展开时，左下角仍保留独立的“场所一览”按钮，形成两个并列组件。
- 调整后：`scene-navigator`
  是唯一导航组件；收起态只显示按钮，展开态隐藏按钮并在完全相同的
  左下锚点向上展开面板。
- 桌面端展开面板的左边缘和底边分别与收起按钮的左边缘和底边一致；测得底边均为
  `681.60px`。
- 移动端沿用同一状态模型，展开后成为 390px 宽、`72svh`
  高的底部抽屉，上方地图保持可见。
- 账户入口没有生成全屏遮罩；旧全屏容器在账户状态下尺寸仍为 0 × 0。

## 聚焦区域比较

- 字体与层级：沿用原场所面板的楷体标题、无衬线表单文字和现有字号层级，没有新增字体漂移。
- 间距与节奏：按钮态和面板态共用左下锚点；面板内部标题、标签页、表单和主按钮间距未改变。
- 色彩与视觉令牌：继续使用原有米白纸面、深色描边、阴影和绿色主操作色。
- 图像质量：地图背景及其裁切、清晰度和艺术方向保持不变，没有引入或替换图像资产。
- 文案：场所、账户、登录与注册文案保持不变；按浏览器标注移除了登录按钮下方的角色权限说明，
  注册页的居民账户提示保留。

## 交互与运行检查

- 已验证：收起按钮 → 场所列表 → 账户入口 → 登录／注册切换。
- 已验证：账户入口 → 返回场所列表，以及关闭 → 收起按钮。
- 已验证：关闭后组件状态回到 `collapsed`，账户模式回到 `places`。
- 已验证：展开时收起按钮透明度为 0、指针事件为
  `none`，页面不会出现两个可点击入口。
- 已验证：桌面 1280 × 720 与移动端 390 × 844。
- 浏览器控制台没有 `error` 或 `warning`。

## 比较历史

1. 初次内嵌解决了全屏账户页问题，但账户面板和“场所一览”按钮仍同时出现。
2. 将按钮与面板包入 `scene-navigator`，用 `collapsed`／`expanded` 与
   `places`／`account` 两组状态表达同一组件的形态。
3. 重启本地预览后复验：桌面端按钮与面板共用同一底部锚点；移动端展开为底部抽屉；未发现
   新的 P0、P1 或 P2 问题。
4. 对照同一 1280 × 720
   登录状态，删除登录表单底部权限说明；表单结构、主按钮与面板锚点
   均未变化，浏览器确认该 `small` 元素数量为 0。

## Implementation Checklist

- [x] 单一导航组件封装按钮和面板。
- [x] 收起、场所列表、账户入口三态可逆切换。
- [x] 桌面与移动端响应式布局。
- [x] 返回、关闭、焦点和无障碍状态同步。
- [x] 浏览器交互、控制台、测试和构建验证。

## Follow-up Polish

当前没有需要阻断交付的视觉问题。

final result: passed
